有些数据缓冲区在物理内存中可能并不连续（例如通过 kmalloc 分配的大块内存，或者用户态传进来的 buffer）。但是很多外设 要求连续的 DMA 地址区间 来做传输。为了解决这个矛盾，就出现了 scatter-gather (SG) 机制：

* scatter：把分散的物理内存段记录起来。
* gather：在 DMA 传输时，外设或 DMA 控制器能把这些分散的段拼接成一个逻辑连续的数据流。


